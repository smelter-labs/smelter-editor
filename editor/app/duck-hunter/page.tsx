/**
 * /duck-hunter — the arcade itself is rendered by ./layout.tsx (it has to
 * outlive the /duck-hunter → /duck-hunter/[roomId] URL rewrite; see the
 * comment there). This page only exists so the route matches.
 */
export default function DuckHunterPage() {
  return null;
}
